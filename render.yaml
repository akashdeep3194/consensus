services:
  - type: web
    name: consensus-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: consensus-db
          property: connectionString
      - key: DEV_LOGIN
        value: "0"
      - key: SESSION_SECRET
        generateValue: true
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: GOOGLE_REDIRECT_URI
        value: "https://consensus-api.onrender.com/api/auth/google/callback"
      - key: ADMIN_TOKEN
        sync: false

databases:
  - name: consensus-db
    databaseName: consensus
    plan: free